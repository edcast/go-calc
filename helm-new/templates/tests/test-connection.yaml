apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-new.fullname" . }}-test-connection"
  labels:
    {{- include "helm-new.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-new.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
