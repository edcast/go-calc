apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-new.fullname" . }}
  labels:
    {{- include "helm-new.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-new.selectorLabels" . | nindent 4 }}
